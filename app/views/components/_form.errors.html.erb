<% if instance.present? and instance.errors.present? %>

<div class="container">
<section class="hero is-small is-danger">
<div class="hero-body">

    <p class="title">
        Oh snap! we are unable to create your article. Please check the following
    </p>

    <ul>
        <% instance.errors.full_messages.each do |err| %>
            <li><%= err %></li>
        <% end %>
    </ul>

</div>
</section>
</div>

<% end %>